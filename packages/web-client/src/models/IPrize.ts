// TODO: Models need to be in a shared code location
export interface IPrize {
  id: number;
  name: string;
  description: string;
  initialStock: number;
  currentStock: number;
  weight: number;
}
